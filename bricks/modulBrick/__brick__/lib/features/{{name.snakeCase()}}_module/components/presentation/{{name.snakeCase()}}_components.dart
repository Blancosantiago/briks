import 'package:flutter/material.dart';

class {{name.pascalCase()}}Components extends StatelessWidget {
  const {{name.pascalCase()}}Components({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}